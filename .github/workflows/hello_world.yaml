name: Hello World workflow

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    hello:
        runs-on: LNL-PW00N65F
        steps:
            - uses: actions/checkout@v2
            - name: hello world
              run: write-host "hello World"
              shell: powershell
    
    goodbye:
        runs-on: LNL-PW00N65F
        steps:
            - name: goodbye world
              run: write-host "goodbye World"
              shell: powershell